<h3>Render a list of users and their activity</h1>

<% users.forEach(function(post, i) {  %>}
	<div class="user-listing">
		<h4> 
			<a href="/users/<%=user._id%>"> <%=user.username%> </a>
		</h4>
	</div>
<% }) %>
